{
  "name": "roots",
  "version": "0.1.0",
  "description": "A learning system for pi that remembers: teaches from unconditional truths upward, then schedules what you learned with FSRS before you forget it.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "pi-package",
    "learning",
    "spaced-repetition",
    "fsrs"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ca-ke/roots.git"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "agents": "tsx scripts/agents.ts",
    "test": "tsx test/smoke.ts && tsx test/typeset.ts",
    "lint": "tsx scripts/complexity.ts",
    "typecheck": "tsc",
    "check": "npm run typecheck && npm run lint && npm test && npm run agents -- --check"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "//peerDependenciesMeta": "pi bundles these and resolves them itself. Marked optional so npm 7+ does not auto-install a second, quickly-stale copy of the whole agent when this package is installed.",
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": {
      "optional": true
    },
    "@earendil-works/pi-coding-agent": {
      "optional": true
    },
    "typebox": {
      "optional": true
    }
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.3",
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typebox": "^1.3.19",
    "typescript": "^5.7.2"
  }
}
